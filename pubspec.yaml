name: flutter_dec
description: A Flutter project with command processing capabilities.
version: 1.0.0+9

environment:
  sdk: ">=2.17.0 <3.0.1"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
